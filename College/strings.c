#include<stdio.h>
void main()
{
  char a[20], n, i;

  // String using Arrays 
  // printf("Enter the length of the string(Not more than 19): ");
  // scanf("%d" , &n);
  // printf("Enter the String: ");
  // for(i=0;i<n;i++)
  // {
  //   scanf("%c",&a[i]);
  // }
  // a[i]='\0';

  // String Using str 

  // printf("Enter a String: ");
  // scanf("%s",a);
  // printf("%s",a);

  // Strings Using fgets() function 

  // printf("Enter the String: ");
  // fgets(a,20,stdin);
  // puts(a);
  
}